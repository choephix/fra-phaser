{
  "name": "fra-phaser",
  "version": "0.2.0",
  "description": "just some furry robots",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "tsc:w": "tsc -w",
    "lite": "lite-server --host 0.0.0.0",
    "start": "concurrently \"npm run tsc:w\" \"npm run lite\" "
  },
  "author": "cx",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "lite-server": "^2.6.1",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  }
}
